# Web framework + ASGI server
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Settings & validation
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# Database (SQLAlchemy ORM + drivers)
SQLAlchemy==2.0.36
# Postgres driver (used in production / Neon). psycopg v3 binary build.
psycopg[binary]==3.2.3

# Auth
PyJWT==2.10.1
bcrypt==4.2.1
# Google SSO — verifies Google ID tokens server-side
google-auth==2.37.0
requests==2.32.3

# Tests (dev) — kept here so `pip install -r requirements.txt` sets you up to run them
pytest==8.3.4
httpx==0.28.1
